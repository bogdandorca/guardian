.container-fluid.col-md-12
    .page-title.col-md-12
        h1.col-md-6 Login
    .page-body.col-md-12
        form.form-group.col-md-6.col-md-offset-3(name="loginForm")
            .col-md-12(ng-show="error")
                .alert.alert-danger {{error}}

            .col-md-12
                label Email Address
                input(type="text", name="email", ng-model="userCredentials.email", placeholder="john.doe@nookie.com", required).form-control
                div(class="errorContainer", ng-show="loginForm.email.$dirty && loginForm.email.$invalid")
                    small(class="errorMessage", ng-show="loginForm.firstName.$error.required") Your email is required

            .col-md-12
                label Password
                input(type="password", name="password", ng-model="userCredentials.password", placeholder="Password", required).form-control
                div(class="errorContainer", ng-show="loginForm.password.$dirty && loginForm.password.$invalid")
                    small(class="errorMessage", ng-show="loginForm.password.$error.required") Your password is required

            .col-md-12
                btn.btn.btn-primary(type="submit", ng-disabled="loginForm.$invalid", ng-submit="login()", ng-click="login()") Login
                a(href="/register").btn.btn-warning Register